# as this is the first file/script to execute, start tracing here
import os
from dotenv import load_dotenv
load_dotenv()
import streamlit as st

st.title("Showcasing possibilities of Azure Open AI when used with other Azure services")
